language: java

jdk:
  - oraclejdk8

build:
  ci:
    - mkdir -p shippable/testresults
    - mkdir -p shippable/codecoverage
    - ant test
    - ls
    - mv TEST-CalculatorTest.xml shippable/testresults/

  post_ci:
    - echo 'post_ci'

  on_success:
    - echo 'success :)'

  on_failure:
    - echo 'fail :('
